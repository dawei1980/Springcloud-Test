spring:
  application:
    name: spring-login
  profiles:
    active: dev
  rabbitmq:
    host: localhost
    port: 32775
    username: guest
    password: guest

---
spring:
  profiles: dev
  application:
    name: config-single-client
  cloud:
    config:
      uri: http://localhost:8082 #config server 端地址
      label: master
      profile: dev
    bus:
      trace:
        enabled: true
      enabled: true
